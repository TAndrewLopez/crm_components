type Props = {
    children: React.ReactNode;
};

const SubmissionsLayout = ({ children }: Props) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default SubmissionsLayout;
